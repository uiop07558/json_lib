#include "../lib/data_structures/hashtable/map_c.h"
#include "../include/value.h"
#include "../include/value_map_def.h"

#define __max(a, b) ((a) > (b) ? (a) : (b))
#define __min(a, b) ((a) < (b) ? (a) : (b))

DS_UM_INIT_TYPE_CODE(json_Value, value)
